# VeriFrame Blender Plugin Requirements

# Core dependencies
requests>=2.31.0
typing>=3.7.4

# Future Starknet integration (commented out for now as they need to be installed in Blender's Python)
# starknet-py>=0.21.0
# cairo-lang>=0.12.0

# Development dependencies (for testing outside of Blender)
# pytest>=7.0.0
# black>=22.0.0